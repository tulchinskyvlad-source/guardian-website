const menuBtn=document.querySelector('.menu-button');const nav=document.querySelector('.nav-links');
if(menuBtn){menuBtn.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));});}
document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>nav?.classList.remove('open')));
const form=document.getElementById('requestForm');
if(form){const panels=[...form.querySelectorAll('.panel')],bars=[...form.querySelectorAll('.progress span')];let step=0;
const show=n=>{step=Math.max(0,Math.min(n,panels.length-1));panels.forEach((p,i)=>p.classList.toggle('active',i===step));bars.forEach((b,i)=>b.classList.toggle('active',i<=step));form.scrollIntoView({behavior:'smooth',block:'center'});};
form.querySelectorAll('.choice').forEach(btn=>btn.addEventListener('click',()=>{form.querySelectorAll('.choice').forEach(x=>x.classList.remove('selected'));btn.classList.add('selected');document.getElementById(btn.dataset.field).value=btn.dataset.value;}));
form.querySelectorAll('.next-step').forEach(btn=>btn.addEventListener('click',()=>{if(step===0&&!document.getElementById('requester').value){alert('Please select who the ride is for.');return;}if(step===1&&(!document.getElementById('pickup').value.trim()||!document.getElementById('destination').value.trim())){alert('Please enter pickup and destination.');return;}show(step+1);}));
form.querySelectorAll('.prev-step').forEach(btn=>btn.addEventListener('click',()=>show(step-1)));
form.addEventListener('submit',e=>{e.preventDefault();const g=id=>document.getElementById(id)?.value?.trim()||'Not provided';const text=`Guardian Ride Request\nRequester: ${g('requester')}\nName: ${g('name')}\nPhone: ${g('phone')}\nEmail: ${g('email')}\nPickup: ${g('pickup')}\nDestination: ${g('destination')}\nDate: ${g('tripDate')}\nPickup time: ${g('pickupTime')}\nTrip type: ${g('tripType')}\nTransportation need: ${g('vehicle')}\nNotes: ${g('notes')}\n\nI understand this request is not confirmed until Guardian responds.`;document.getElementById('requestText').textContent=text;document.getElementById('smsLink').href=`sms:9179002068?&body=${encodeURIComponent(text)}`;document.getElementById('confirmation').classList.add('visible');window.guardianRequest=text;});
document.getElementById('copyRequest')?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(window.guardianRequest||'');document.getElementById('copyRequest').textContent='Copied';}catch{alert('Copy failed. Select the request text and copy it manually.');}});}
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('shown')}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
